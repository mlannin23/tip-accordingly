<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Tip Accordingly" %>">
    <meta name="author" content="Michael Lannin">
    <%= csrf_meta_tags %>

    <title><%= content_for?(:title) ? yield(:title) : "Tip Accordingly" %></title>

    <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
  </head>
  <body>
    <%= render 'layouts/messages' %>

    <%= yield %>

    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  </body>
</html>
